# Public canonical URL used for SEO metadata and sitemap generation.
NEXT_PUBLIC_SITE_URL=https://docs.example.com
# Public base URL for the separately deployed Express API. It is intentionally
# limited to the API address; database and Judge0 credentials remain backend-only.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
